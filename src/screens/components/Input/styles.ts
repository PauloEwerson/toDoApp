import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputText: {
    backgroundColor: '#1F1E25',
    borderRadius: 5,
    height: 56,
    width: '100%',
    color: '#fff',
    padding: 16,
    fontSize: 16,
  }
});