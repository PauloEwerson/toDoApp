import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    alignItems: 'center',
  },
  containerInput: {
    flexDirection: 'row',
    width: '100%',
    padding: 20,
  }
});